# OctoFit Tracker (scaffold)

This folder contains a minimal scaffold for the OctoFit Tracker application.

- `backend/requirements.txt`: Python package requirements for the Django backend.

Follow the project instructions in the repository to set up the virtual environment and install dependencies.
